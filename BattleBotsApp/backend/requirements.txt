# Pinned to minimum versions that support Python 3.12 / 3.13 / 3.14.
# (Lower bounds, not exact pins, so pip can resolve compatible builds.)

# Web framework
fastapi>=0.115.5
uvicorn[standard]>=0.32.1

# HTTP clients
httpx>=0.27.2

# HTML parsing
beautifulsoup4>=4.12.3
lxml>=5.3.0

# Data validation & settings (>=2.11 ships pydantic-core built against
# PyO3 0.24+, which is the first release with Python 3.14 support).
pydantic>=2.11.0
pydantic-settings>=2.7.0

# Environment variables
python-dotenv>=1.0.1

# LLM clients (used in Phase 4 prediction engine)
openai>=1.55.0
anthropic>=0.39.0

# Testing
pytest>=8.3.3
pytest-asyncio>=0.24.0
